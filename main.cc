#include <iostream>
#include <string>
using namespace std;
int main(){
	string s1, s2;
	while (true){
		cin >> s1;
		if (s1 == "q"){
			cout << "quit" << break;
		}
		if (s1 == "no"){
			continue;
		}			
	}
}
